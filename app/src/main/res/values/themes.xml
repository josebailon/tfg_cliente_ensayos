<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Ensayos" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!--   Primary Color -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!--   darker variant for the status bar and contextual app bars -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!--   theme UI controls like checkboxes and text fields -->
        <item name="colorAccent">@color/colorAccent</item>
        <item name="materialButtonStyle">@style/Theme.Ensayos.Button</item>

        <item name="android:windowSplashScreenBackground">@color/white</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/animicono_mini_largo</item>
        <item name="android:windowSplashScreenAnimationDuration">3000</item>
        <item name="windowSplashScreenIconBackgroundColor">#f0e067</item>
    </style>


    <style name="Theme.Ensayos" parent="Base.Theme.Ensayos" />


    <style name="Theme.Ensayos.Toolbar" parent="Widget.Material3.Toolbar">
        <item name="android:textColorSecondary">#FFFFFF</item>
    </style>

    <style name="Theme.Ensayos.Button" parent="Widget.MaterialComponents.Button">
        <item name="cornerSize">50dp</item>
    </style>
</resources>